# Тестовое задание ITR - Лента постов

Fullstack приложение на Yii2 + React для работы с лентой постов.

## 🛠 Технологии

### Backend
- Yii2 Framework
- PHP 8.2+
- MySQL
- Composer

### Frontend
- React 18
- TypeScript
- Vite
- Tailwind CSS

## 📋 Требования

- PHP >= 8.2
- Composer
- Node.js >= 20
- MySQL/MariaDB
- Apache/Nginx (с mod_rewrite для Apache)

## 🚀 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone git@github.com:ark1700/itr-test.git
cd itr-test
```

### 2. Настройка Backend

#### 2.1 Установка зависимостей

```bash
cd backend
composer install
```

#### 2.2 Настройка базы данных

Создайте базу данных MySQL:

```sql
CREATE DATABASE itr_test CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

Настройте подключение в `backend/config/db.php`:

```php
return [
    'class' => 'yii\db\Connection',
    'dsn' => 'mysql:host=localhost;dbname=itr_test',
    'username' => 'root',
    'password' => '',
    'charset' => 'utf8mb4',
];
```

#### 2.3 Выполнение миграций

```bash
php yii migrate
```

#### 2.4 Запуск сервера (для разработки)

```bash
# Встроенный PHP сервер
php yii serve --port=8080
```

Backend будет доступен по адресу: `http://localhost:8080`

### 3. Настройка Frontend

#### 3.1 Установка зависимостей

```bash
cd frontend
npm install
```

#### 3.2 Настройка переменных окружения

Отредактируйте `.env`:

```env
VITE_API_BASE_URL=http://localhost:8080
```

#### 3.3 Запуск сервера разработки

```bash
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:5173`

## 📖 Использование

### REST API Endpoints

**Получить список постов:**
```
GET /api/posts?sort={type}
```

Параметры сортировки:
- `random` - случайный порядок
- `oldest_first` - старые наверху
- `newest_first` - новые наверху

**Создать пост:**
```
POST /api/posts
Content-Type: application/json

{
  "title": "Заголовок поста",
  "body": "Текст поста"
}
```

### Основные функции

1. **Создание поста** - форма с полями заголовка и текста
2. **Просмотр ленты** - отображение всех постов
3. **Сортировка** - 3 варианта отображения постов
4. **Автообновление** - лента обновляется после создания поста
5. **SPA** - работа без перезагрузки страницы

## 🏗 Структура проекта

### Backend

```
backend/
├── config/          # Конфигурация приложения
├── controllers/     # REST контроллеры
├── models/          # Модели данных
├── services/        # Бизнес-логика
├── migrations/      # Миграции БД
├── tests/           # Тесты (Codeception)
└── web/             # Публичная директория
```

### Frontend

```
frontend/
├── src/
│   ├── api/         # API клиент
│   ├── components/  # React компоненты
│   ├── types/       # TypeScript типы
│   └── lib/         # Утилиты
└── public/          # Статические файлы
```
